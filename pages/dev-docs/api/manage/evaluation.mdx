
import Swagger from '../swagger';

# API: evaluation

The `/api/v1/evaluation` routes are a collection of routes that are used to create, read, update, and delete [Fides evaluations](/docs/dev-docs/cli/check/cmd-evaluate). This can be used to integrate with popular dashboard and reporting tools to get an overview on repo-level Privacy evaluations within your organization. 

<br></br>

<Swagger 
    filterStrings={[
        {
            'path': '/api/v1/evaluation',
            'greedy': 'true'
        }
    ]} 
    defaultModelsExpandDepth={-1}
/>
