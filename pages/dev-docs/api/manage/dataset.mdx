import Swagger from '../swagger';

# API: dataset

The `/api/v1/dataset` routes are a collection of routes that are used to create, read, update and delete [Dataset](/docs/dev-docs/configuration/integrations/database-integrations/datasets) objects.

<br></br>

<Swagger 
    filterStrings={[
        {
            path: '/api/v1/dataset',
            greedy: true
        }
    ]} 
    defaultModelsExpandDepth={-1}
/>
