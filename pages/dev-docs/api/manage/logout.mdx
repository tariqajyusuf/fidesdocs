
import Swagger from '../swagger';

# API: logout

The `/api/v1/logout` route is used to end a session within Fides and remove the client object. 

<br></br>

<Swagger 
    filterStrings={[
        {
            'path': '/api/v1/logout',
            'greedy': 'true'
        }
    ]} 
    defaultModelsExpandDepth={-1}
/>
