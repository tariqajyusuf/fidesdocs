
import Swagger from '../swagger';

# API: connection

The `/api/v1/connection` routes are a collection of routes that are to create, read, update, and delete Connections within Fides. [Database and SaaS Integrations](/dev-docs/configuration/integrations) are represented by the `Connection` endpoint on the API level. 

<br></br>

<Swagger 
    filterStrings={[
        {
            'path': '/api/v1/connection',
            'greedy': 'true'
        }
    ]} 
    defaultModelsExpandDepth={-1}
/>
