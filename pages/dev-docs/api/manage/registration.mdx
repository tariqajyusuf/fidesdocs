
import Swagger from '../swagger';

# API: registration

The `/api/v1/registration` routes show the Fides registraiton status with Ethyca. 

<br></br>

<Swagger 
    filterStrings={[
        {
            'path': '/api/v1/registration',
            'greedy': 'true'
        }
    ]} 
    defaultModelsExpandDepth={-1}
/>
