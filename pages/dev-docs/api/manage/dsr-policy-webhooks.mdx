import Swagger from '../swagger';

# API: dsr/policy/\{policy_key}/webhook

The `/api/v1/dsr/policy/\{policy_key}/webhook` routes are a collection of routes that are used to [configure Privacy Requests Webhooks](/docs/dev-docs/configuration/privacy-requests/webhooks). This is a powerful tool that allows you to enrich identities (or handle pre/post events) when performing [graph traversal](/docs/dev-docs/configuration/privacy-requests/graphs).

<br></br>

<Swagger 
    filterStrings={[
        {
            'path': '/api/v1/dsr/policy/{policy_key}/webhook/pre_execution',
        },
        {
            'path': '/api/v1/dsr/policy/{policy_key}/webhook/post_execution',
        },
        {
            'path': '/api/v1/dsr/policy/{policy_key}/webhook/pre_execution/{pre_webhook_key}',
        },
        {
            'path': '/api/v1/dsr/policy/{policy_key}/webhook/post_execution/{post_webhook_key}',
        },
    ]} 
    defaultModelsExpandDepth={-1}
/>
