import Swagger from '../swagger';

# API: consent-request

The `/api/v1/consent-request` routes are a collection of routes that use to create, read, and update consent-request objects.

<br></br>

<Swagger 
    filterStrings={[
        {
            'path': '/api/v1/consent-request',
            'greedy': 'true'
        }
    ]} 
    defaultModelsExpandDepth={-1}
/>
