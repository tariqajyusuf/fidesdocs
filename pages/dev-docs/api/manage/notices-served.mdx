
import Swagger from '../swagger';

# API: notices-served

The `/api/v1/notices-served` route records notices served to a Fides's user device id only. 

<br></br>

<Swagger 
    filterStrings={[
        {
            'path': '/api/v1/notices-served',
            'greedy': 'true'
        }
    ]} 
    defaultModelsExpandDepth={-1}
/>
