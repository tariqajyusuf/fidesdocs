
import Swagger from '../swagger';

# API: connection_type

The `/api/v1/connection_type` routes are a collection of routes that are used to represent the different types of connections available within the Fides UI. Objects in this endpoint will be represented in the [Integrations UI](/user-guides/integrations).

<br></br>

<Swagger 
    filterStrings={[
        {
            'path': '/api/v1/connection_type',
            'greedy': 'true'
        }
    ]} 
    defaultModelsExpandDepth={-1}
/>
