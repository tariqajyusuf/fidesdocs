
import Swagger from '../swagger';

# API: data_qualifier

The `/api/v1/data_qualifier` routes are used to to create, read, update, and delete the Data Qualifiers within the Fides platform. Data qualifers Data qualifers can be customized but we recommend you use the [Fideslang](https://ethyca.github.io/fideslang/explorer/) taxonomy.

<br></br>

<Swagger 
    filterStrings={[
        {
            'path': '/api/v1/data_qualifier',
            'greedy': 'true'
        }
    ]} 
    defaultModelsExpandDepth={-1}
/>
