
import Swagger from '../swagger';

# API: access_manual_webhook

The `/api/v1/access_manual_webhook` route lists out all manual webhooks configured within Fides. Manual Webhooks are configured within the [Connection](/docs/dev-docs/api/manage/connection) endpoint.
<br></br>

<Swagger 
    filterStrings={[
        {
            'path': '/api/v1/access_manual_webhook',
            'greedy': 'true'
        }
    ]} 
    defaultModelsExpandDepth={-1}
/>
