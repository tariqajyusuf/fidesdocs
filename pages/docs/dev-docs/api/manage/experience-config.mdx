
import Swagger from '../swagger';

# API: experience-config

The `/api/v1/experience-config` routes are a collection of routes that are used to create, read, update, and delete [Privacy Experience](/tutorials/consent-management-fundamentals/consent-management-privacy-experiences) configurations.

<br></br>

<Swagger 
    filterStrings={[
        {
            'path': '/api/v1/experience-config',
            'greedy': 'true'
        }
    ]} 
    defaultModelsExpandDepth={-1}
/>
