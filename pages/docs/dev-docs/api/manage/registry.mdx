
import Swagger from '../swagger';

# API: registry

The `/api/v1/registry` routes are a collection of routes that are used create, read, update, and delete registry objects. 

<br></br>

<Swagger 
    filterStrings={[
        {
            'path': '/api/v1/registry',
            'greedy': 'true'
        }
    ]} 
    defaultModelsExpandDepth={-1}
/>
