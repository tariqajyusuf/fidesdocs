
import Swagger from '../swagger';

# API: Validate

The `/api/v1/validate` routes are a collection of routes that are used to validate different resource targets.

<br></br>

<Swagger 
    filterStrings={[
        {
            'path': '/api/v1/validate',
            'greedy': 'true'
        }
    ]} 
    defaultModelsExpandDepth={-1}
/>
